# ==========================================
# Gift Instance Manager Configuration
# ==========================================

# ─── Server Configuration ───────────────────────────────────
PORT=4000
NODE_ENV=development

# ─── Authentication ─────────────────────────────────────────
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# ─── Docker Configuration ───────────────────────────────────
# Docker socket path (usually /var/run/docker.sock on Linux)
DOCKER_SOCKET_PATH=/var/run/docker.sock

# Docker image for gift tracker instances
GIFT_TRACKER_IMAGE=gift-tracker:latest

# Port range for instances (manager will auto-assign ports in this range)
INSTANCE_PORT_START=3000
INSTANCE_PORT_END=4000

# ─── Backend API (for instances to connect to) ──────────────
BACKEND_API_URL=https://o3-ttgifts.com/api/instances

# ─── Database ───────────────────────────────────────────────
DATABASE_PATH=./data/instances.db

# ─── Optional: Nginx Configuration ──────────────────────────
NGINX_CONFIG_PATH=/etc/nginx/sites-available/
DOMAIN=app.o3-ttgifts.com
